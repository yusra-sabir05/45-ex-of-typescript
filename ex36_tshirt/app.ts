import { Console } from "console"

                
function make_shirt(size : string,message:string ) {
    console.log(`making a ${size} t-shirt with the message ${message} printed on it`)
}
make_shirt("small","sunsets and good vibes")

